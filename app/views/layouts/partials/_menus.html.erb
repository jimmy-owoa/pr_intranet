<section class="section">
  <h5 class="section-header">Menus</h5>
  <div class="table-responsive">
    <table class='table table-sm'>
      <thead>
      <tr>
        <th>Título</th>
        <th>Descripción</th>
        <th>CSS Class</th>
        <th>Prioridad</th>
        <th>Padre</th>
        <th>Link - URL</th>
        <th colspan="3"></th>
      </tr>
      </thead>
      <tbody>
        <% @menus.each do |user| %>
          <tr>
            <td><%= user.title %></td>
            <td><%= user.description %></td>
            <td><%= user.css_class %></td>
            <td><%= user.priority%></td>
            <td><%= user.parent_id %></td>
            <td><%= link_to "Link", eval("#{user.link}") %></td>
            <td></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</section>