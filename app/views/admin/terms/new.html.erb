<section class="section">
  <h1 class="section-header">Nueva etiqueta</h1>
  <%= simple_form_for @term, as: 'term', url: {action: :create}, :html => { :class => "form" } do |f| %>
    <%= render :partial => 'form', locals: {f: f} %>
  <% end %>
</section>
