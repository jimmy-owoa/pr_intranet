<section class="section">
  <h1 class="section-header"><%= t('.create_category') %></h1>
  <%= simple_form_for @benefit_type, as: 'benefit_type', url: {action: :create}, :html => { :class => "form" } do |f| %>
    <%= render :partial => 'form', locals: {f: f} %>
  <% end %>
</section>
