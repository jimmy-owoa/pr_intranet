<section class="section">
  <h1 class="section-header">Nuevo nacimiento</h1>
  <%= simple_form_for @birth, as: 'birth', url: {action: :create}, :html => { :class => "form" } do |f| %>
    <%= render :partial => 'form', locals: {f: f} %>
  <% end %>
</section>
