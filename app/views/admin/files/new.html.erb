<section class="section">
  <h1 class="section-header">Crear Archivo</h1>
  <%= simple_form_for @file, as: 'file', url: {action: :create}, :html => { :class => "form" } do |f| %>
      <%= render :partial => 'form', locals: {f: f} %>
  <% end %>
</section>
