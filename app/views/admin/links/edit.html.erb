<section class="section">
  <h1 class="section-header">Editar</h1>
  <%= simple_form_for @link, as: 'link', url: {action: :update}, :html => { :class => "form" } do |f| %>
    <%= render :partial => 'form', locals: {f: f} %>
  <% end %>
</section>
