<%= f.error_notification %>
<div class="row form-group">
  <div class="col-6">
    <div class="row">
      <div class="col-12 col-sm-10">
        <%= f.input :title, autofocus: true   %>
      </div>
      <div class="col-12 col-sm-10">
        <%= f.input :css_class, as: :minicolors, input_html: {data: {
          minicolors: {theme: :bootstrap, position: :right}}} %>
      </div>
      <div class="col-12 col-sm-10">
        <%= f.input :code %>
      </div>
      <div class="col-12 col-sm-10">
        <%= f.input :priority %>
      </div>            
    </div>
  </div>
  <div class="col-6">
    <div class="row">
      <div class="col-12 col-sm-10">
          <%= f.input :description, :as => :text %>
      </div>
      <div class="col-12 col-sm-10">
        <%= f.label 'Padre' %>
        <%= f.select :parent_id,  General::Menu.where(parent_id: nil).map{ |m| [m.title, m.id] }, include_blank: true%>
      </div>
      <div class="col-12 col-sm-10"></br>
        <%= f.input :link %>
      </div>            
    </div>
  </div>  
  <div class="col-6 col-sm-6">
    <%= f.label :categories %>
    <%= select_tag :term_names, options_for_select(General::Term.categories.map(&:name), @menu.terms.categories.map(&:name)), multiple: true, class:'for_select form-control string optional' %>
  </div>  
</div>
    <%= f.submit nil, :class => 'btn btn-primary'%>