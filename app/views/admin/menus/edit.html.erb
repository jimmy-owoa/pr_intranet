<section class="section">
  <h1 class="section-header">Editar menú <%= @menu.title %></h1>
  <%= simple_form_for @menu, as: 'menu', url: {action: :update}, :html => { :class => "form" } do |f| %>
    <%= render :partial => 'form', locals: {f: f} %>
  <% end %>
</div>
