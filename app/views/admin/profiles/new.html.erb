<section class="section">
  <h1 class="section-header">Crear Grupo</h1>
  <%= simple_form_for @profile, as: 'profile', url: {action: :create}, :html => { :class => "form" } do |f| %>
      <%= render :partial => 'form', locals: {f: f} %>
  <% end %>
</section>
