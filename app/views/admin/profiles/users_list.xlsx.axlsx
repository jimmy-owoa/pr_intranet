wb = xlsx_package.workbook
wb.add_worksheet(name: "Lista de usuarios") do |sheet|
  sheet.add_row ['Nombre', 'Rut', 'email']
  @profile.users.each do |user|
    sheet.add_row [user.full_name, "#{user.legal_number}-#{user.legal_number_verification}", user.email]
  end
end