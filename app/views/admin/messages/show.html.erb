<section class="section">
  <h1 class="section-header">Link: <%= @message.id %></h1>
  <div class="container card">
    <div class="col-md-12" align="center">
      <h4>Título: <a href='<%= @message.title %>'><%= @message.title %></a></h4>
      <div style='width: 400px; height: 400px;'>
        <%=image_tag @message.image.variant(resize: "360x300>") if @message.image.attached?%>
      </div>
    </div>
  </div>
  <br><br>
</section>

    <div align="center">
      <%= link_to t('.back', :default => t("helpers.messages.back")), admin_messages_path, :class => 'btn btn-primary'  %>
      <%= link_to t('.edit', :default => t("helpers.messages.edit")), edit_admin_message_path(@message), :class => 'btn btn-primary' %>
    </div>