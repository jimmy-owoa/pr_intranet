<section class="section">
  <h1 class="section-header">Crear Mensaje</h1>
  <%= simple_form_for @message, as: 'message', url: {action: :create}, :html => { :class => "form" } do |f| %>
    <%= render :partial => 'form', locals: {f: f} %>
  <% end %>
</section>
